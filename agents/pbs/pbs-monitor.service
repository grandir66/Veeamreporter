[Unit]
Description=PBS Backup Monitor
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/bin/python3 /opt/backup-monitor/pbs_monitor.py -c /etc/backup-monitor/pbs-config.yaml

[Install]
WantedBy=multi-user.target
